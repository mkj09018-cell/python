# 액션 플랜 (Action Plan): 재팬 로컬로그 개발

본 액션 플랜은 PRD를 바탕으로 웹 애플리케이션을 단계별로 구축하기 위한 계획입니다. 웹 기반의 최적화된 UI/UX를 제공하기 위해, Streamlit 대신 더 빠르고 가시성이 좋은 모던 웹 기술(React/Tailwind)로 진행합니다.

## Phase 1: 기본 UI 프레임워크 및 라우팅 구축 (디자인 기초 단계)
1. **사이드바 네비게이션 구현**: 좌측에 "재팬 로컬로그" 6개 카테고리 탭(이모티콘 포함) 및 라우팅 설정.
2. **색상 및 테마 설정**: 파스텔 톤의 노란색/민트색을 글로벌 Tailwind 테마에 적용.
3. **빈 페이지 스캐폴딩**: 6개 카테고리에 해당하는 기본 페이지 구조(Layout) 생성.

## Phase 2: 파이어베이스 및 관리자 인가(Auth) 세팅
1. **Firebase 연동**: 데이터 저장을 위한 Firestore 세팅 (플랫폼 내 내장 도구 `set_up_firebase` 활용).
2. **관리자 로그인 설정**: 푸터(Footer) 영역에 관리자 로그인 구현. 네이버 로그인 API를 검토하여 연동하거나, 필요시 가장 안전한 Firebase Auth(Google) 방식을 도입 후 특정 이메일('mkj09018@gmail.com' 등)에만 에디터 권한 부여.

## Phase 3: 메인 웹 기능 구현 (데이터베이스 연동 전)
1. **메인 화면 (Home)**: 네이버 블로그형 레이아웃, 일본 지도 사진 첨부, 실시간 TOP 5 랭킹 공간(목업 데이터) 마련.
2. **환율 정보 카테고리**: 무료 환율 API 활용하여 실시간 JPY-KRW 환율 표시 및 환율 계산기 로직 구현.
3. **번역기 & 사전 카테고리**: 오픈 API(또는 구글 번역/MyMemory 무료버전) 기반 번역 UI 구성 및 자주 쓰는 회화 사전 리스트(정적 데이터) 작성.
4. **뉴스 카테고리**: 네이버 API(또는 오픈 RSS)를 통해 "일본 재난, 날씨, 범죄" 키워드 필터링 뉴스 패널 구현, 우측 추천 앱 및 유튜브 링크 패널 배치.
5. **지역별 날씨 카테고리**: 일본 주요 지역 무료 날씨 API(OpenWeather 등) 통신, 지역 검색바 및 7일 예보 UI 구현.

## Phase 4: 지도 기반 인터랙션 & 데이터베이스 데이터 뷰 구성 (Firestore 활용)
1. **게시물 카테고리**: SVG 일본 전도 구현 (15개 지역 마커 추가 및 툴팁 표출).
2. **추천집 카테고리**: 도쿄, 츠쿠바 탭 분리, Firestore의 맛집 데이터를 불러와 리스트/페이지네이션 렌더링.
3. **좋아요 기능 연동**: 추천집 매장마다 하트 토글 및 "좋아요한 식당보기" 필터 로직 구현.

## Phase 5: 관리자 전용 CMS (관리 페이지) 구현 마무리
1. 관리자 권한으로 로그인된 상태일 때만 보이는 **글쓰기 패널 / 수정 버튼** 컴포넌트 추가.
2. Firestore DB에 게시글 전송, 여행지 마커 데이터 업로드, 추천 맛집 추가 기능 구현.

---
**진행 가이드**: 이 액션 플랜 컨펌 하시면, Phase 1 ~ Phase 2부터 곧바로 폴더 구조 세팅과 UI 디자인 작업을 시작하겠습니다.
